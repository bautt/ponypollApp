# KV Store collection for poll questions
[ponypoll_questions]
field.sort_order = number
field.text = string
field.type = string
field.timeLimit = number
field.image = string
field.options_json = string
accelerated_fields.by_order = sort_order

# KV Store collection for app configuration (index selection, etc.)
[ponypoll_config]
field.poll_index = string
field.poll_subject = string
